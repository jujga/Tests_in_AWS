pipeline {
    agent any
    stages {
        stage('Run tests using docker') {
            withPythonEnv('python3') {
                sh 'pip install pytest'
                sh 'pytest mytest.py'
    }
        }
}

}